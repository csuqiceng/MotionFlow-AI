{
  "name": "motionflow-ai-desktop",
  "private": true,
  "version": "0.1.0",
  "productName": "MotionFlow AI",
  "author": {
    "name": "MotionFlow AI"
  },
  "description": "Electron shell for MotionFlow AI (bundles a PyInstaller-built robot server)",
  "main": "build/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && set ELECTRON_DEV=1&& .\\node_modules\\electron\\dist\\electron.exe .",
    "start": "electron .",
    "dist": "electron-builder --config electron-builder.yml --win nsis --x64",
    "verifyRelease": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\verify-release.ps1",
    "smokePackagedRobotServer": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\smoke-packaged-robot-server.ps1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "cross-env": "^7.0.3",
    "electron": "^33.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.6.0"
  }
}
