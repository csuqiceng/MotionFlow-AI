{
  "name": "nanobot-robot-ai-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Electron shell for nanobot Robot AI (bundles a PyInstaller-built gateway)",
  "main": "build/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && cross-env ELECTRON_DEV=1 electron .",
    "start": "electron .",
    "dist": "electron-builder --config electron-builder.yml --win nsis"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "cross-env": "^7.0.3",
    "electron": "^33.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.6.0"
  }
}
