# Node / Electron build artifacts
node_modules/
build/
release/

# Python build venv (per-machine, ~hundreds of MB)
.build-venv/

# PyInstaller outputs (regenerated by build-desktop.ps1)
pyinstaller/dist/
pyinstaller/build/

# Local smoke-test scratch
.smoke-config.json
*.log
